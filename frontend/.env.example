# Frontend environment variables (commit this example to repo)
# URL the frontend should use to reach the backend when running in Docker
NEXT_PUBLIC_API_URL=http://backend:8000
# Fallback when running on host machine (overrides nothing, only used by code if provided)
NEXT_PUBLIC_API_FALLBACK=http://localhost:8000
